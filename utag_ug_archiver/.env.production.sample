# Production deployment settings
DJANGO_SECRET_KEY=yGwuAy#l9k36ms*3f&o8hRBhK8MEPvvM8XD-RdrmW$sK00Z-1#NN(MNys^4_dii6
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=utag.ug.edu.gh,utagweb,localhost
CSRF_TRUSTED_ORIGINS=https://utag.ug.edu.gh

# PostgreSQL running inside docker-compose.app.yml (service name: db)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=app_database
DB_USER=db_user
DB_PASSWORD=change-me
DB_HOST=db
DB_PORT=5432
DB_CONN_MAX_AGE=60
# Optionally override with a full URL if pointing to an external DB
# DATABASE_URL=postgres://db_user:strong-password@db-host:5432/app_database

# (Optional) keep SQLite settings handy for quick experiments
# DB_ENGINE=django.db.backends.sqlite3
# SQLITE_NAME=/app/db.sqlite3

# Redis / Celery endpoints
REDIS_URL=redis://redis:6379/1
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Static & media paths on the container
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles
MEDIA_URL=/media/
MEDIA_ROOT=/app/media

# Email delivery (example uses SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=1
EMAIL_USE_SSL=0
EMAIL_HOST_USER=you@example.com
EMAIL_PASSWORD=app-password
DEFAULT_FROM_EMAIL="UTAG UG <no-reply@example.com>"

# Docker compose knobs
WEB_PORT=8000
RUN_MIGRATIONS=1
RUN_COLLECTSTATIC=1

# Reverse proxy & TLS
NGINX_SERVER_NAME=utag.ug.edu.gh
WEB_SERVICE_HOST=web
WEB_SERVICE_PORT=8000
CERTBOT_EMAIL=admin@utag.ug.edu.gh
# Seconds between automated renewal attempts (default 12h)
CERTBOT_RENEW_INTERVAL=43200
